<?php echo $this->listenerName; ?> = function(field,e){
    newUrl = <?php echo $this->newUrl; ?>
    prox = Pk+'_proxy';
    gri = Pk+'_grid';
    proxies = Ext.getCmp(gri).getStore();
    proxies.proxy = new Ext.data.HttpProxy({
        url: newUrl
         });
    Ext.getCmp(gri).getStore().reload();

}