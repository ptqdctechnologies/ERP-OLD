<script type="text/javascript">

    Ext.onReady(function(){

        Ext.QuickTips.init();

        <?php echo $this->action('index','textfield',null); ?>


        function create()
        {
            myPanel = Ext.getCmp('rate-panel');
            myPanel.body.load({
                url: '/finance/rateidr/create',
                scripts : true
            });
        }

        /*function edit()
        {
            myPanel = Ext.getCmp('rate-panel');
            myPanel.body.load({
                url: '/finance/rateidr/edit',
                scripts : true
            });
        }*/
        
        function view()
        {
            myPanel = Ext.getCmp('rate-panel');
            myPanel.body.load({
                url: '/finance/rateidr/view',
                scripts : true
            });
        }

        var windowForm = ({

            title:'Master Rate',
            id:'rate-panel',
            layout: 'fit',
            bodyCfg : { cls:'xpanel-body-table' , style: {'overflow':'auto'}},
            bodyStyle: 'padding:15px;',
            loadMask: true,
            minWidth: 300,
            minHeight: 200,
            tbar:[
                {
                    xtype: 'tbbutton',
                    text: 'Select Action',
                    iconCls: 'menu-drop',
                    menu: {
                    items: [
                            {
                                id: 'masterwork_add_button',
                                text: 'Input Rate',
                                cls:"x-btn-text-icon" ,
                                icon:"/images/icons/fam/action_paste.gif",
                                handler:create
                            }
                            /*,{
                                id: 'masterwork_edit_button',
                                text: 'Edit Rate',
                                cls:"x-btn-text-icon" ,
                                icon:"/images/icons/fam/page_edit.gif",
                                handler:edit
                            }*/
                            ,{
                                id: 'masterwork_view_button',
                                text: 'View Rate',
                                cls:"x-btn-text-icon" ,
                                icon:"/images/icons/fam/grid.png",
                                handler:view
                            }
                            ]
                    }
                }
            ]

        })

        viewPort = Ext.getCmp('viewport');

        centerRegionTab = viewPort.getComponent('content-panel');
        centerRegionTab.items.each(function(c){centerRegionTab.remove(c);});
        centerRegionTab.add(windowForm);
        panelBaru = Ext.getCmp('rate-panel');
        centerRegionTab.layout.setActiveItem('rate-panel');
        panelBaru.body.load({
                url: '/finance/rateidr/view',
                scripts: true
            });

        showSubmenu('rate-panel');
    })

</script>
