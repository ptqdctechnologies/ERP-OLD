<script type="text/javascript">

    Ext.onReady(function(){

        Ext.QuickTips.init();

        <?php echo $this->action('index','textfield',null); ?>

        var windowForm = ({

            title:'Finance Periode',
            id:'abs-budget-panel',
            layout:'fit',
            bodyCfg : { cls:'xpanel-body-table' , style: {'overflow':'auto'}},
            bodyStyle: 'padding:15px;',
            loadMask: true,
            minWidth: 300,
            minHeight: 200,
            tbar:[{
                xtype:'tbbutton',
                text:'Select Action',
                iconCls:'menu-drop',
                menu:{
                    items:[{
                        id:'add_periode_button',
                        text:'Add Periode',
                        cls:"x-btn-text-icon" ,
                        icon:"/images/icons/fam/action_paste.gif",
                        handler:function ()
                        {
                            panel = Ext.getCmp('abs-budget-panel');
                            panel.body.load ({
                                url: '/finance/periode/setperiode',
                                scripts: true
                            })
                        }
                    },{
                        id:'edit_periode_button',
                        text:'Edit Periode',
                        cls:"x-btn-text-icon" ,
                        icon:"/images/icons/fam/action_paste.gif",
                        handler:function ()
                        {
                            panel = Ext.getCmp('abs-budget-panel');
                            panel.body.load ({
                                url: '/finance/periode/editperiode',
                                scripts: true
                            })
                        }
                    }]
                }
            }]

        })

        viewPort = Ext.getCmp('viewport');

        centerRegionTab = viewPort.getComponent('content-panel');
        centerRegionTab.items.each(function(c){centerRegionTab.remove(c);});
        centerRegionTab.add(windowForm);
        panelBaru = Ext.getCmp('abs-budget-panel');
        centerRegionTab.layout.setActiveItem('abs-budget-panel');
                    panelBaru.body.load({
                        url: '/finance/periode/viewperiode',
                        scripts: true
                    });
                showSubmenu('abs-budget-panel');


    });


</script>