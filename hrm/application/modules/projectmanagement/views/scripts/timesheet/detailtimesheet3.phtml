<link rel="stylesheet" type="text/css" href="/css/table.css"/>
<style>
.title {
	width: 100%;
	text-align: center;
	font-size: 18px;
	font-weight: bold;
	margin-bottom: 10px;
}
</style>
<div class="body">
<div class="title">TIMESHEET SUMMARY</div>
<h2>Periode : <?=$this->periode?></h2>
<h2>Date : <?=$this->tgl1?> - <?=$this->tgl2?></h2>
<h2>Total Hour in Periode : <?=$this->totalHour?> Hours</h2>
<br></br>
<table id="mytable" cellspacing="0">
        <tr>
            <th scope="col"><center>No</center></th>
            <th scope="col"><center>NPK</center></th>
            <th scope="col"><center>Name</center></th>
            <th scope="col"><center>Project</center></th>
            <th scope="col"><center>Hour</center></th>
            <th scope="col"><center>Total Hour Person</center></th>
            <th scope="col"><center>Total Hour HRD</center></th>
            <th scope="col"><center>Percentage Person</center></th>
            <th scope="col"><center>Percentage HRD</center></th>
        </tr>
    	<?php
    		$totals = 0;
    		foreach($this->result as $key => $val)
            {
    	?>
    		<tr>
    		<th scope="row" class="spec"><?=($no+1)?></th>
    		<td><?=$val['npk']?></td>
    		<td><?=$val['name']?></td>
    		<td><?=$val['prj_kode'] . " - " . $val['prj_nama']?></td>
    		<td align="right"><?=number_format(floatval($val['hour']))?></td>
    		<td align="right"><?=number_format(floatval($val['totalhour1']))?></td>
    		<td align="right"><?=number_format(floatval($this->totalHour))?></td>
    		<td align="right"><?=number_format(floatval($val['persen']),2) . "%"?></td>
    		<td align="right"><?=number_format(floatval($val['persen2']),2) . "%"?></td>
    		</tr>
    	<?php
                $totals += floatval($val['hour']);
                $no++;
    		}

    	?>
    	<tr>
    		<th scope="row" class="spec" colspan="4">Grand Total</th>
    		<td align="right"><?=number_format($totals)?></td>
    	</tr>
</table>

</div>